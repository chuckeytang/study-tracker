export const bigCheckRadius = 100;
export const majornodeRadius = 80;
export const minornodeRadius = 60;

export const clusterBaseDistance = 650; // 基础距离
export const clusterMinDistance = 100; // 最小距离
export const clusterDistanceDecrease = 200; // 每层递减的距离

export const bigCheckSpacingX = 1800; // bigCheckNode之间的X轴间距
export const bigCheckBaseY = 0; // bigCheckNode的基础Y轴位置
export const bigCheckYOffset = 200; // bigCheckNode之间的Y轴偏移量

export const handlerRadius = 2;
